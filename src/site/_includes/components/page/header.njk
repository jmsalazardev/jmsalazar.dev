
<header class="mb-4 
    {% if props.bg -%}
    {{ props.bg }}
    {%- else -%}
     bg-indigo-700
    {%- endif -%}
    "
>
    <div class="px-4 py-8 md:py-16 lg:py-24">
        <h1 class="text-center text-3xl text-gray-50">{{ props.title }}</h1>
        <p class="text-center py-2 text-gray-200">{{ props.description }}</p>
    </div>
</header>