---
layout:  base.njk
tmplClass: simple
---
<div class="content">
    <div class="bg"></div>
    <main>

        <article>

            
            <h1>{{ title | heading | safe }}</h1>
            <p class="excerpt">{{ data.excerpt | safe }}</p>

            <div class="divider"></div>

            <img src="{{ image }}" alt="{{ title }}"/>

            <div class="toc-container">
                <h2>Tabla de contenidos</h2>
                {{ content | toc | safe }}
            </div>

            <div class="content-article">
                {{ content | safe}}
            </div>

        </article>>
    </main>
</div>
